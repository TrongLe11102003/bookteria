server:
  port: 8084
  servlet:
    context-path: /file
spring:
  application:
    name: file-service
  data:
    mongodb:
      uri: mongodb://root:root@localhost:27017/file-service?authSource=admin
app:
  file:
    storage-dir: /Users/trongle/Documents/Tài liệu/upload
    download-prefix: http://localhost:888/api/v1/file/media/
